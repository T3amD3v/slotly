{
  "version": 2,
  "buildCommand": "rm -rf .next && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "routes": [
    { "src": "/api/auth/(.*)", "dest": "/api/auth/$1" }
  ]
} 